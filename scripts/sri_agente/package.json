{
  "name": "sri-agente",
  "version": "1.0.0",
  "description": "Agente de descargas SRI — corre en la PC del operador (IP residencial)",
  "main": "agente.js",
  "bin": "agente.js",
  "scripts": {
    "start": "node agente.js",
    "build": "pkg agente.js --targets node18-win-x64 --output dist/sri_agente.exe"
  },
  "pkg": {
    "scripts": [
      "../sri_scraper.js"
    ],
    "assets": [
      "../node_modules/playwright-core/**/*"
    ]
  },
  "comentario": "En desarrollo el agente reutiliza las dependencias de scripts/node_modules (playwright-extra, stealth). Para empaquetar con pkg se usa el Chrome/Edge del sistema (channel), por lo que NO se incluyen los binarios de navegador de Playwright."
}
